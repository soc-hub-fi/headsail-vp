#include <stdint.h>
#include <string.h>
#include <stdio.h>


int main()
{
    printf("Hello, world! (from newlib)\n\r");   
    int num;
    // gets(name);
    scanf("%i", &num);
    // printf("Hello %s!\n", name);

    return 0;
}
