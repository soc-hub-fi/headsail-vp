name: Validate

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CI: 1
  CARGO_TERM_COLOR: always
  RENODE_DIR: /opt/renode/
  # Used by renode-test to generate snapshots of failed tests
  RENODE_CI_MODE: YES
  DLA_BIN: dla

jobs:
  build-dla-example:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Install requirements
      run: rustup target add riscv64imac-unknown-none-elf
    - name: Build examples
      working-directory: ./examples/hpc/hello-dla
      run: cargo build --example dla
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: $DLA_BIN
        path: ./examples/hpc/target/riscv64imac-unknown-none-elf/debug/examples/dla
        if-no-files-found: error
        retention-days: 14

  run-dla-example:
    needs: build-dla-example

    runs-on: ubuntu-latest
    container:
      image: antmicro/renode:1.14.0
      options: --user root

    steps:
    - uses: actions/checkout@v4
    - name: Download artifact
      uses: actions/download-artifact@v4
      with:
        name: $DLA_BIN
    - name: Create Renode peripheral symlinks
      run: ln -s $(readlink -f "./vp/devel/python_peripherals/DLA.py") "$RENODE_DIR/scripts/pydev/DLA.py"
    - name: Run example
      run: renode-test scripts/robot/is_hello.robot --variable BIN:"$(readlink -f $DLA_BIN)"
    - name: Upload snapshots
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        path: snapshots/

  build-ffi:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Install requirements
      run: rustup target add riscv64imac-unknown-none-elf riscv32imc-unknown-none-elf
    - name: Build FFI without RT on SysCtrl
      working-directory: ./examples/headsail-bsp-ffi
      run: cargo build --target=riscv32imc-unknown-none-elf -Fpanic-uart
    - name: Build FFI without RT on HPC
      working-directory: ./examples/headsail-bsp-ffi
      run: cargo build --target=riscv64imac-unknown-none-elf -Fpanic-uart

  build-minimal-c:
    runs-on: ubuntu-latest

    env:
      RISCV: /opt/riscv/
      GCC_VER: "2024.04.12"
      RISCV_XLEN: riscv64
      UBUNTU_VER: ubuntu-22.04
    steps:
    - uses: actions/checkout@v4
    - name: Download GCC
      working-directory: /root/riscv-gnu-toolchain/
      run: |
        wget -q https://github.com/riscv-collab/riscv-gnu-toolchain/releases/download/$GCC_VER/$RISCV_XLEN-elf-ubuntu-$UBUNTU_VER-llvm-nightly-$GCC_VER-nightly.tar.gz
        mkdir ${RISCV} && \
          tar -xf $RISCV_XLEN-elf-ubuntu-$UBUNTU_VER-llvm-nightly-$GCC_VER-nightly.tar.gz --strip-components 1 -C $RISCV
        export PATH="${RISCV}/bin:${PATH}"
        $RISCV_XLEN-unknown-elf-gcc --version
    - name: Build minimal C project
      working-directory: ./examples/hpc-c/hello-hpc
      run: just build
    - name: Upload binary
      uses: actions/upload-artifact@v4
      with:
        name: hello-hpc-c.elf
        path: ./examples/hpc-c/hello-hpc/build/hello-hpc
        if-no-files-found: error
        retention-days: 14

  build-ffi-test:
    needs: build-minimal-c

    runs-on: ubuntu-latest
    env:
      RISCV: /opt/riscv/
      GCC_VER: "2024.04.12"
      RISCV_XLEN: riscv64
      UBUNTU_VER: ubuntu-22.04
    steps:
    - uses: actions/checkout@v4
    - name: Download GCC
      working-directory: /root/riscv-gnu-toolchain/
      run: |
        wget -q https://github.com/riscv-collab/riscv-gnu-toolchain/releases/download/$GCC_VER/$RISCV_XLEN-elf-ubuntu-$UBUNTU_VER-llvm-nightly-$GCC_VER-nightly.tar.gz
        mkdir ${RISCV} && \
          tar -xf $RISCV_XLEN-elf-ubuntu-$UBUNTU_VER-llvm-nightly-$GCC_VER-nightly.tar.gz --strip-components 1 -C $RISCV
        export PATH="${RISCV}/bin:${PATH}"
        $RISCV_XLEN-unknown-elf-gcc --version
    - name: Build minimal C project
      working-directory: ./examples/hpc-c/test-bsp-ffi
      run: just build
    - name: Upload binary
      uses: actions/upload-artifact@v4
      with:
        name: test-bsp-ffi.elf
        path: ./examples/hpc-c/test-bsp-ffi/build/test-bsp-ffi
        if-no-files-found: error
        retention-days: 14

  run-minimal-c:
    needs: build-minimal-c

    runs-on: ubuntu-latest
    container:
      image: antmicro/renode:1.14.0
      options: --user root

    steps:
    - uses: actions/checkout@v4
    - name: Download binary
      uses: actions/download-artifact@v4
      with:
        name: hello-hpc-c.elf
    - name: Create Renode peripheral symlinks
      run: ln -s $(readlink -f "./vp/devel/python_peripherals/DLA.py") "$RENODE_DIR/scripts/pydev/DLA.py"
    - name: Run example
      run: renode-test scripts/robot/is_hello.robot --variable BIN:"$(readlink -f hello-hpc-c.elf)"
    - name: Upload snapshots
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        path: snapshots/

  run-ffi-test:
    needs: build-ffi-test

    runs-on: ubuntu-latest
    container:
      image: antmicro/renode:1.14.0
      options: --user root

    steps:
    - uses: actions/checkout@v4
    - name: Download binary
      uses: actions/download-artifact@v4
      with:
        name: test-bsp-ffi.elf
    - name: Create Renode peripheral symlinks
      run: ln -s $(readlink -f "./vp/devel/python_peripherals/DLA.py") "$RENODE_DIR/scripts/pydev/DLA.py"
    - name: Run example
      run: renode-test scripts/robot/is_hello.robot --variable BIN:"$(readlink -f test-bsp-ffi.elf)"
    - name: Upload snapshots
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        path: snapshots/
